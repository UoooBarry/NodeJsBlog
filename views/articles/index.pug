extends ../layout

block content
    a(href='articles/add') Create an article
    ul(class='list-group list-group-flush')
        each article, i in articles
            li(class='list-group-item')
                p
                    b
                        a(href='articles/' + article._id)= article.title
                p= article.author
                p= article.content